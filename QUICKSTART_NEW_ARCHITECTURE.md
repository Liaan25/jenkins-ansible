# 🚀 Quick Start: Новая архитектура с RLM

**Версия:** 2.0  
**Дата:** 2025-11-24

---

## ✅ Что изменилось?

### Раньше (НЕ работало):
- ❌ Ansible пытался сам запустить Vault Agent
- ❌ Использовались неправильные пользователи
- ❌ User Systemd вместо System Systemd

### Сейчас (РАБОТАЕТ):
- ✅ Vault Agent устанавливается через RLM API
- ✅ Разные пользователи для Vault Agent и Monitoring
- ✅ Vault Agent = System service, Monitoring = User services
- ✅ Полная автоматизация

---

## 👥 Архитектура пользователей

```
kae_stend = CI10742292 (пример)

┌─────────────────────────────────────────────────────┐
│ Vault Agent (RLM)         Monitoring (IDM)          │
├─────────────────────────────────────────────────────┤
│ CI10742292-lnx-va-start   CI10742292-lnx-mon_sys    │
│ CI10742292-lnx-va-read    CI10742292-lnx-mon_ci     │
│                           CI10742292-lnx-mon_admin  │
│                           CI10742292-lnx-mon_ro     │
└─────────────────────────────────────────────────────┘
```

---

## 🎯 Пошаговая инструкция

### ШАГ 1: Создать пользователей Monitoring через IDM

**Создать заявку в IDM на:**
- `{{ kae_stend }}-lnx-mon_sys` — Сервисный пользователь (СУЗ)
- `{{ kae_stend }}-lnx-mon_ci` — CI/CD пользователь (ТУЗ)
- `{{ kae_stend }}-lnx-mon_admin` — Администратор (ПУЗ)
- `{{ kae_stend }}-lnx-mon_ro` — ReadOnly

**Пример для `kae_stend = CI10742292`:**
```
CI10742292-lnx-mon_sys
CI10742292-lnx-mon_ci
CI10742292-lnx-mon_admin
CI10742292-lnx-mon_ro
```

**Проверка:**
```bash
getent passwd CI10742292-lnx-mon_sys
```

---

### ШАГ 2: Синхронизировать Bitbucket с GitHub

Если вы работаете с корпоративным Bitbucket:

```bash
# На вашей рабочей станции
cd /path/to/secure_deployment
git pull origin master      # Получить последние изменения с GitHub
git push bitbucket master   # Отправить в корпоративный Bitbucket
```

**Проверка:**
- Открыть Bitbucket → проверить наличие коммита `ef994b0`

---

### ШАГ 3: Настроить Jenkins Credentials

**Необходимые Credentials:**

| ID | Тип | Описание |
|----|-----|----------|
| `rlm-token` | Secret text | Токен для RLM API |
| `ssh-key-mvp-dev` | SSH Private Key | SSH ключ для `mvp_dev` |
| `vault-approle-monitoring` | Secret file | Vault AppRole credentials |

**Как добавить RLM токен:**
1. Jenkins → Credentials → (global)
2. Add Credentials → Secret text
3. Secret: `<ваш RLM токен>`
4. ID: `rlm-token`
5. Save

---

### ШАГ 4: Запустить Jenkins Pipeline

**Параметры:**

| Параметр | Значение | Пример |
|----------|----------|--------|
| `SERVER_ADDRESS` | IP целевого сервера | `10.100.200.50` |
| `NAMESPACE_CI` | Namespace из Vault | `KPRJ_01_01_000000_01` |
| `SSH_CREDENTIALS_ID` | ID SSH ключа | `ssh-key-mvp-dev` |
| `DEBUG` | `true` | Для детальных логов |
| `DEPLOYMENT_METHOD` | `ansible` | — |

**KAE_STEND извлечется автоматически из NAMESPACE_CI!**

Пример:
```
NAMESPACE_CI = KPRJ_01_01_000000_01
       ↓
KAE_STEND = CI10742292  (извлечено в Jenkins)
       ↓
Пользователи:
  - CI10742292-lnx-va-start (Vault Agent, создаст RLM)
  - CI10742292-lnx-mon_sys  (Monitoring, должен существовать)
```

---

### ШАГ 5: Мониторинг выполнения

**Этапы Pipeline:**

1. ✅ **Валидация параметров** (2 сек)
2. ✅ **Проверка пользователей/групп** (5 сек)
3. ✅ **Получение секретов из Vault** (10 сек)
4. ✅ **Подготовка Ansible проекта** (5 сек)
5. ✅ **Передача секретов на сервер** (10 сек)
6. ⏳ **Развертывание (Ansible)** — **НОВОЕ!**
   - 6.1. Common Setup (1 мин)
   - 6.2. **RLM: Установка Vault Agent** (10-15 мин) ← НОВОЕ
   - 6.3. **RLM: Установка RPM** (5 мин × 3) ← НОВОЕ
   - 6.4. Проверка Vault Agent (1 мин)
   - 6.5. Настройка Prometheus (2 мин)
   - 6.6. Настройка Grafana (2 мин)
   - 6.7. Настройка Harvest (2 мин)
7. ✅ **Проверка безопасности** (опционально)

**Общее время:** ~30-40 минут (из-за RLM заявок)

---

## 🔍 Проверка результата

### На целевом сервере:

```bash
# 1. Проверка пользователей Vault Agent (созданы RLM)
getent passwd CI10742292-lnx-va-start
getent group CI10742292-lnx-va-read

# 2. Проверка пользователей Monitoring (созданы IDM заранее)
getent passwd CI10742292-lnx-mon_sys
getent group CI10742292-lnx-mon_sys

# 3. Проверка Vault Agent (System service)
sudo systemctl status vault-agent
sudo journalctl -u vault-agent -n 50

# 4. Проверка сертификатов
ls -lh /opt/vault/certs/

# 5. Проверка Monitoring services (User services)
sudo -i -u CI10742292-lnx-mon_sys
systemctl --user status prometheus
systemctl --user status grafana
systemctl --user status harvest

# 6. Проверка работы (открыть в браузере)
# Prometheus: https://<server>:9090
# Grafana: https://<server>:3000
```

---

## 🚨 Troubleshooting

### Ошибка: "User CI10742292-lnx-mon_sys does not exist"

**Причина:** Monitoring пользователи не созданы через IDM

**Решение:**
1. Создать заявку в IDM (см. ШАГ 1)
2. Дождаться одобрения
3. Перезапустить Pipeline

---

### Ошибка: "RLM task failed"

**Причина:** Проблема с RLM API (неверный токен, недоступность сервиса)

**Решение:**
1. Проверить токен: Jenkins → Credentials → `rlm-token`
2. Проверить доступность RLM: `curl https://rlm.delta.sbrf.ru`
3. Посмотреть детали в логах Jenkins (DEBUG=true)

---

### Ошибка: "Vault Agent service not found"

**Причина:** RLM не установил Vault Agent (возможно, заявка не прошла IDM одобрение)

**Решение:**
1. Проверить логи Ansible (ЭТАП 1.5 → RLM Installation)
2. Найти ID заявки RLM в логах
3. Проверить статус заявки: `https://rlm.delta.sbrf.ru/api/tasks/<id>/`
4. Если статус `failed` → посмотреть причину в RLM UI

---

### Ошибка: "Permission denied" в /dev/shm

**Причина:** Старые файлы с неправильными правами

**Решение:**
```bash
# Очистить вручную
sudo rm -rf /dev/shm/monitoring_secrets
```

Затем перезапустить Pipeline.

---

## 📚 Документация

| Документ | Описание |
|----------|----------|
| `VAULT_AGENT_ARCHITECTURE.md` | Полная архитектура системы |
| `CHANGES_VAULT_AGENT_FIX.md` | Описание изменений v2.0 |
| `CORPORATE_SECURITY_RULES.md` | Правила безопасности |
| `DYNAMIC_GROUP_SUMMARY.md` | Архитектура единой группы |
| `SUDOERS_FOR_MVP_DEV.md` | sudoers правила |

---

## 🎉 Готово!

После успешного развертывания:

1. ✅ Vault Agent работает от `{{ kae_stend }}-lnx-va-start`
2. ✅ Prometheus/Grafana/Harvest работают от `{{ kae_stend }}-lnx-mon_sys`
3. ✅ Все сервисы используют TLS сертификаты из Vault
4. ✅ Логи пишутся в `/opt/monitoring/logs/`
5. ✅ Данные в `/opt/monitoring/data/`

**Для мониторинга 500 серверов:**
- Просто запустите Pipeline 500 раз с разными `SERVER_ADDRESS`
- Или используйте Jenkins Multibranch / Declarative Pipeline с параметризацией
- Все полностью автоматизировано, без ручного вмешательства!

---

**Версия:** 2.0 (2025-11-24)  
**Коммит:** `ef994b0`  
**Статус:** ✅ Production Ready




