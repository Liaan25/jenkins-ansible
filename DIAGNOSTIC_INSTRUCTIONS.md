# 🔍 Диагностика проблемы с доступом к бинарным файлам

## Проблема

Сервисы не запускаются, хотя символические ссылки созданы. Символические ссылки всегда имеют права `777` и владельца `root` - это нормально.

## Реальная причина

Пользователь сервиса (`CI10742292-lnx-mon_sys`) **не имеет доступа к целевым бинарным файлам**.

## Диагностика

### 1. Проверить права доступа на целевые бинарные файлы

```bash
# Проверить права на целевые файлы
sudo ls -la /usr/sbin/grafana-server
sudo ls -la /usr/bin/prometheus  
sudo ls -la /opt/harvest/bin/harvest

# Пример вывода:
# -rwxr-xr-x 1 root root 12345678 Nov 25 18:51 /usr/sbin/grafana-server
# -rwxr-xr-x 1 root root 87654321 Nov 25 18:51 /usr/bin/prometheus
# -rwxr-xr-x 1 root root 23456789 Nov 25 18:51 /opt/harvest/bin/harvest
```

### 2. Проверить доступ пользователя сервиса

```bash
# Проверить может ли пользователь сервиса читать файлы
sudo -u CI10742292-lnx-mon_sys ls -la /usr/sbin/grafana-server
sudo -u CI10742292-lnx-mon_sys ls -la /usr/bin/prometheus
sudo -u CI10742292-lnx-mon_sys ls -la /opt/harvest/bin/harvest

# Проверить может ли пользователь сервиса выполнять файлы
sudo -u CI10742292-lnx-mon_sys /usr/sbin/grafana-server --version
sudo -u CI10742292-lnx-mon_sys /usr/bin/prometheus --version
sudo -u CI10742292-lnx-mon_sys /opt/harvest/bin/harvest version
```

### 3. Проверить группы пользователя сервиса

```bash
# Проверить группы пользователя сервиса
sudo groups CI10742292-lnx-mon_sys

# Проверить группы целевых файлов
ls -la /usr/sbin/grafana-server | awk '{print $4}'
ls -la /usr/bin/prometheus | awk '{print $4}'
ls -la /opt/harvest/bin/harvest | awk '{print $4}'
```

## Решение

### Если пользователь не имеет доступа

Создать RLM заявку для добавления пользователя в соответствующие группы:

1. **Тип заявки**: `UVS_LINUX_ADD_USERS_GROUP`
2. **Пользователь**: `CI10742292-lnx-mon_sys`
3. **Группы**: [указать группы из диагностики]
4. **Сервер**: `tvlds-mvp001939`

### Пример RLM заявки

```json
{
  "action_type": "UVS_LINUX_ADD_USERS_GROUP",
  "users": "CI10742292-lnx-mon_sys",
  "group_name": "[группа_из_диагностики]",
  "server_list": "tvlds-mvp001939"
}
```

## Проверка после исправления

```bash
# После согласования RLM заявки
sudo -u CI10742292-lnx-mon_sys /usr/sbin/grafana-server --version
sudo -u CI10742292-lnx-mon_sys /usr/bin/prometheus --version
sudo -u CI10742292-lnx-mon_sys /opt/harvest/bin/harvest version

# Запустить сервисы
sudo -u CI10742292-lnx-mon_sys bash -c 'XDG_RUNTIME_DIR=/run/user/$(id -u) systemctl --user start grafana'
sudo -u CI10742292-lnx-mon_sys bash -c 'XDG_RUNTIME_DIR=/run/user/$(id -u) systemctl --user status grafana'
```

## Важные моменты

- ✅ **Символические ссылки всегда `root:root 777`** - это нормально
- ✅ **Права доступа определяются целевым файлом**
- ✅ **Пользователь сервиса должен иметь доступ к целевым файлам**
- ✅ **Использовать RLM для добавления в группы** (не менять владельца файлов)
- ✅ **Соответствие корпоративным правилам безопасности**
