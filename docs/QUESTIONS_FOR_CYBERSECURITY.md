# ❓ Вопросы для согласования с кибербезопасностью

> Профессиональные вопросы по архитектурным решениям системы мониторинга

**Дата:** November 19, 2024  
**Проект:** Автоматизированное развертывание системы мониторинга  
**Заявка IDM:** №25/341016

---

## 1. Vault Agent Templates для секретов

**Контекст:**  
Vault Agent автоматически генерирует конфигурационные файлы приложений с встроенными credentials через механизм templates. Секреты никогда не попадают в переменные окружения или stdout.

**Пример template:**
```hcl
template {
  destination = "/opt/monitoring/config/grafana_datasource.yaml"
  contents = <<EOT
apiVersion: 1
datasources:
  - name: Prometheus
    basicAuthUser: {{ with secret "path/to/creds" }}{{ .Data.user }}{{ end }}
    secureJsonData:
      basicAuthPassword: {{ with secret "path/to/creds" }}{{ .Data.pass }}{{ end }}
EOT
  perms = "0640"
}
```

**Вопрос:**  
Является ли данный подход (Vault Agent templates с прямой генерацией конфигов) корректным с точки зрения безопасности, или требуется альтернативное решение?

---

## 2. SHA256 хеши в sudoers для скрипт-оберток

**Контекст:**  
Все команды с динамическими параметрами (iptables, curl к RLM, обработка сертификатов) обернуты в bash-скрипты с валидацией. Sudoers содержат SHA256 хеши для гарантии неизменности:

```sudoers
Cmnd_Alias FIREWALL_WRAPPER = sha256:abc123...def456 /opt/monitoring/scripts/wrappers/manage_firewall.sh
CI_USER ALL=(ALL:ALL) NOPASSWD: FIREWALL_WRAPPER
```

**Вопрос:**  
Достаточно ли SHA256 хеша для защиты скриптов от подмены, или требуется дополнительная мера безопасности (например, цифровая подпись скриптов)?

---

## 3. Whitelist валидация в скрипт-обертках

**Контекст:**  
Скрипты-обертки используют whitelist подход для валидации всех входных параметров:

```bash
# manage_firewall.sh
declare -A ALLOWED_SERVICES=(
    ["prometheus"]="9090"
    ["grafana"]="3000"
    ["harvest_unix"]="12990"
)

validate_service() {
    if [[ ! -v ALLOWED_SERVICES["$service"] ]]; then
        echo "ERROR: Invalid service"
        return 1
    fi
}
```

**Вопрос:**  
Является ли whitelist подход с фиксированным списком допустимых значений достаточной мерой валидации, или требуется более строгая проверка (например, дополнительная regex валидация параметров)?

---

## 4. Установка ПО из tar.gz vs RPM

**Контекст:**  
Для полного соответствия правилу "не изменять файлы пакетного менеджера" предлагается установка Prometheus, Grafana, Harvest из официальных tar.gz архивов в `/opt/monitoring/`, вместо RPM пакетов в `/etc/`, `/var/`, `/usr/`.

**Преимущества:**
- Нет изменений системных файлов из RPM
- Полный контроль над конфигурацией
- Соответствие принципу "непривилегированные FS"

**Вопрос:**  
Соответствует ли установка из официальных tar.gz архивов (не из RPM) корпоративной политике, если данное ПО официально распространяется в таком формате и не доступно в портале ДИ?

---

## 5. Процесс обновления скрипт-оберток

**Контекст:**  
При изменении скрипта-обертки изменяется его SHA256 хеш, что требует:
1. Пересчета хеша
2. Обновления sudoers файла
3. Новой заявки в IDM на изменение sudo прав

**Вопрос:**  
Какой процесс согласования требуется для обновления скрипт-оберток (и соответственно SHA256 хешей в sudoers)? Требуется ли полное повторное согласование или возможен упрощенный процесс для minor изменений?

---

## 6. RLM API через скрипт-обертку

**Контекст:**  
Обращение к RLM API для автоматического добавления пользователей в группы выполняется через скрипт-обертку с валидацией:

```bash
# call_rlm_api.sh
readonly RLM_BASE_DOMAIN="rlm.sigma.sbrf.ru"

validate_rlm_url() {
    if [[ ! "$url" =~ ${RLM_BASE_DOMAIN} ]]; then
        return 1  # Reject
    fi
    if [[ ! "$url" =~ ^https:// ]]; then
        return 1  # Only HTTPS
    fi
}

validate_token() {
    if [[ ! "$token" =~ ^[a-zA-Z0-9_-]+$ ]]; then
        return 1  # Reject non-alphanumeric
    fi
}
```

**Вопрос:**  
Является ли корректным обращение к RLM API через скрипт-обертку с whitelist валидацией endpoint и токена, или требуется альтернативный подход для интеграции с RLM?

---

## 7. Альтернатива скрипт-оберткам при отклонении

**Контекст:**  
Если скрипт-обертки будут отклонены, возможны альтернативные варианты:

**Вариант A:** Явные команды без переменных (потребуется ~50-100 строк в sudoers для каждого порта/IP)

**Вариант B:** RPM-пакет со скриптами (требует сборки и публикации в репозиторий)

**Вариант C:** Полностью ручное управление firewall через заявки в SberInfra

**Вопрос:**  
Какой альтернативный подход вы рекомендуете, если скрипт-обертки с SHA256 хешами не будут согласованы?

---

## 8. User Systemd Units для vendor ПО

**Контекст:**  
RPM пакеты Grafana/Prometheus устанавливают системные юниты в `/etc/systemd/system/` и конфиги в `/etc/`. Изменение этих файлов запрещено. Предлагается:

1. Установка из tar.gz в `/opt/monitoring/`
2. User Systemd Units в `~/.config/systemd/user/`
3. Все конфиги в `/opt/monitoring/config/`

**Вопрос:**  
Если по каким-либо причинам невозможно использовать User Systemd Units и установку из tar.gz, какие минимальные sudo права требуются для работы с системными units vendor ПО (Grafana/Prometheus из RPM)?

---

## 9. NOEXEC флаг для read-only команд

**Контекст:**  
Все команды только для чтения (status, просмотр логов) содержат флаг NOEXEC:

```sudoers
USER ALL=(SYS:SYS) NOPASSWD: NOEXEC: /usr/bin/systemctl --user status prometheus
USER ALL=(SYS:SYS) NOPASSWD: NOEXEC: /usr/bin/journalctl --user -u prometheus
```

**Вопрос:**  
Требуется ли флаг NOEXEC также для других просмотровых команд (id, groups, getent), или достаточно только для systemctl/journalctl?

---

## 10. Двухэтапный процесс развертывания

**Контекст:**  
Реализован двухэтапный процесс:

**Этап 1 (PREPARE):**
- Генерация скриптов
- Вычисление SHA256
- Создание sudoers
- Сохранение для IDM

**Этап 2 (DEPLOY):**
- Полное развертывание (после согласования IDM)

**Вопрос:**  
Является ли данный подход (предварительная генерация sudoers для согласования) приемлемым для корпоративного процесса, или существует более оптимальный workflow?

---

## 11. Переменные в команде bash -c

**Контекст:**  
Для выполнения сложных операций от имени service user используется:

```sudoers
CI_USER ALL=(SYS_USER:SYS_GROUP) NOPASSWD: /usr/bin/bash -c *
```

Это позволяет выполнять цепочки команд (pipe, &&, ||) от service user.

**Вопрос:**  
Является ли `bash -c *` приемлемым с учетом, что CI_USER должен иметь широкие права для деплоя? Или требуется явное указание каждой команды через bash -c с конкретными аргументами?

---

## 12. Принцип наименьших привилегий vs Практичность

**Контекст:**  
Текущая архитектура:
- CI_USER: широкие права для деплоя (sudo -u SYS_USER ALL)
- ADMIN_USER: управление сервисами
- RO_USER: только просмотр

Альтернатива - еще более гранулированное разделение (отдельные пользователи для каждой операции).

**Вопрос:**  
Соответствует ли текущий уровень гранулярности (4 типа УЗ) требованиям принципа наименьших привилегий, или требуется дополнительное разделение?

---

## 📋 Резюме вопросов для быстрого ответа

| № | Вопрос | Рекомендация требуется |
|---|--------|------------------------|
| 1 | Vault Agent templates для секретов | Да / Нет / Альтернатива |
| 2 | SHA256 хеши достаточны? | Да / Нет / Добавить подпись |
| 3 | Whitelist валидация достаточна? | Да / Нет / Усилить |
| 4 | Установка из tar.gz vs RPM | Да / Нет / Только RPM |
| 5 | Процесс обновления скриптов | Полное / Упрощенное |
| 6 | RLM API через обертку | Да / Нет / Альтернатива |
| 7 | Альтернатива оберткам | Вариант A / B / C / Другое |
| 8 | User Units vs System Units | User / System / Гибрид |
| 9 | NOEXEC для всех read-only | Да / Только systemctl/journalctl |
| 10 | Двухэтапный процесс | Да / Нет / Предложения |
| 11 | bash -c * приемлем? | Да / Нет / Ограничить |
| 12 | Текущая гранулярность | Достаточно / Требуется больше |

---

## 📞 Контакты для обратной связи

**DevOps команда:**
- Email: [email]
- Confluence: [link]

**Заявка IDM:**
- №25/341016

**Готовы к обсуждению:**
- Видеозвонок для детального разбора
- Демонстрация работы скриптов
- Дополнительная документация по запросу

---

**Благодарим за сотрудничество в обеспечении безопасности!** 🔒


